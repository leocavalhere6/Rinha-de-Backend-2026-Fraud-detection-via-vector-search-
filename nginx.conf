events {
    worker_connections 2048;
}

http {
    access_log off;
    sendfile on;

    upstream api {
        server api01:9999;
        server api02:9999;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
        }
    }
}
